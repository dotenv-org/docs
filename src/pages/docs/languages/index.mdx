import { Libraries } from '@/components/Libraries'

export const description =
  'Protocol offers fine-tuned JavaScript, Ruby, PHP, Python, and Go libraries to make your life easier and give you the best experience when consuming the API.'

export const sections = [
  { title: 'Official Support', id: 'official-support' },
]

# Language Support

The following languages currently support .env.vault files. Swap in the corresponding dotenv-vault library in your codebase to safely decrypt your .env.vault on application load.

<Libraries />
