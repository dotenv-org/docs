import { ScreenshotBrowser } from '@/components/ScreenshotBrowser'

export const description = 'Compare environments from a single page.'

##### [Features](/docs/features)
# Compare Environments

Compare environments from a single page. {{ className: 'lead' }}

![](/assets/img/features/compare-environments.png)

* See secret differences and spot if any are missing
* Go directly to a secret
* Copy secrets across environments in a couple clicks
